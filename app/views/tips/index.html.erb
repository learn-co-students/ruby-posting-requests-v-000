<h1>My Tips</h1>
<% if @results.size > 0 %>
  <ul>
  <% @results.each do |item| %>
    <li><strong><%= item["venue"]["name"] %></strong><br>
      <%= item["tip"]["text"] %></li>
  <% end %>
  </ul>
<% else %>
  <p>Looks like you haven't made any Foursquare tips yet.</p>
<% end %>
