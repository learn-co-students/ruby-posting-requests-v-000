<h1>My Tips</h1>
<ul>
	<% @results.each do |item| %>
	<li><strong><%= item["venue"]["name"] %></strong>
		<%= item["tip"]["text"] %></li>
		<% end %>
	</ul>
	Back to Search <%= link_to "Back", search_url %>
